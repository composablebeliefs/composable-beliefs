# 2026-06-11: The /position skill lands, and the gate catches its first rot

Where things stood: the desk at thirteen, and the previous chronicle's
closing line - "a515 in a clean context is the natural next thread" - taken
literally. The session opened with the bare id as the whole prompt, which is
becoming the house idiom for resuming from the graph: the directive's deps
and evidence carried everything needed, no handoff prose required.

The work itself was authoring, not engineering: skills/position/SKILL.md,
the settle-time capture skill the 2026-06-10 thread executed by hand and
a515 prescribed mechanizing. The skill encodes the order the trace taught:
the a496 ceremony gate runs before anything is staged (position document
only when the verbatim wording is the artifact; observations route to
/assert; spec-not-stance redirects to schema capture, the a511 precedent),
homing per a492, the a511 section format with claims in codepath walk
order, every anchor through `mix cb.resolve` with loose anchors treated as
authoring failures, a503 three-tier belief refs after extraction, and the
settle-time rule the a518 near-loss taught - capture when the thread
settles, not at wrap-up. It stages documents only; extraction stays with
/assert. Registered via the .claude/skills symlink, picked up by the
harness mid-session.

The story beat: dogfooding the skill's own verification step on the worked
position caught real rot, less than a day old. Both `lib/cb/codepath.ex`
anchors in 2026-06-10-code-as-operational-substrate.md had stopped
resolving - the a512 extraction that built the verification gate had itself
moved `resolve_anchor` and the anchor-not-found constructor into
`CB.Anchor`. The tool whose construction rotted the anchors is the tool
that caught them. Re-pointed to lib/cb/anchor.ex, verified one match each,
the gradient doing exactly what the position's third pending claim says it
should: anchor failure flags for review; nothing silently heals.

The discharge then ran longer than the authoring, and instructively so.
The CLAUDE.md skills line is compiled from the graph, so "mention /position
in CLAUDE.md" meant: supersede a521 with a524 (the five-skill line),
supersede c062 with c063 (the output-target contract whose render_sections
pin belief ids), regenerate. The stale scan immediately flagged c049's dep
on the superseded c062 - the dep repoint went through CB.Belief.Mutation
per the assert-session-fold precedent, paired evidence entries and all.
One prose line, three graph writes, and that is the point: every line of
the agent's config now traces, and the scan catches what a hand edit would
have left dangling. Preflight on c063 reproduced the a519 wall exactly
(17 contract / 35 schema hits, all bare tag-overlap, the single true
claim_overlap being the supersession target) - another specimen for the
calibration directive. t0012 materialized done with notes intact through
the fixed sink, a523's fix holding at its second use.

The close added its own specimen: the retro-pair appends and the discharge
rationale both went through raw CB.Belief.Store scripts again - a522 (the
evidence-append front door) collected a third and fourth instance in one
session. It is now the most-reproved gap on the desk.

What the next session inherits: the desk at twelve. a511 is the natural
next thread - the /position skill cites the format in prose, and the worked
position holds three claims staged "lands under cb:a511" waiting for
exactly that execution. a522 is the strong second, on the same
cheaper-to-fix-than-keep-documenting trajectory a523 followed. Both are
single-thread sized.
