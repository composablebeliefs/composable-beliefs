# Chronicle: the format pins itself

**Span:** 2026-06-12 - one focused thread: a bare id summons a desk item.
**Register:** chronicle (cb:a520) - narrative for the operator; the audit
trail lives in the graph and in commits 15295fd and this close.

## Where things stood

a511 had sat on the desk since 06-11: pin the anchored-position format as
schema beliefs heading toward a contract. The format was already real
everywhere except the graph - the worked position instantiated it, the
/position skill mechanized it, and a496's ceremony rule had redirected it
from a position document to direct schema capture (spec is not stance).
The session opened with the operator typing the id and nothing else - the
graph-as-backlog working exactly as designed: the directive carried enough
grounding (deps, evidence, the staged appendix claims) to resume cold.

## The discharge

Four beliefs through the front door. **cb:a526** is the format core
(directive/schema): the Class header, Anchor lines in the c043 grammar
under Claim sections, one-match resolver verification at authoring time,
claim order as the default codepath walk order. **cb:a527** is the
optional terms block - the one element the /position skill does not yet
mechanize, left optional by design until a worked instance needs it.
**cb:a528** settles the vocabulary (position = epistemic artifact,
codepath = render face, walkthrough retired). **cb:a529** is the
three-tier verification gradient as an inference: provenance integrity,
grounding currency, behavioral validity - anchor checks necessary-only.
All four stayed non-contract per a511's "heading toward a contract"
phrasing: no position-document verifier task exists yet, so escalation
remains a deliberate act on the a488 pattern. All seven worked-instance
anchors plus the appendix anchors re-verified green before authoring.

Two incidents, both feeding standing desk items:

**The mis-tag avalanche (a519's third specimen).** The first preflight
pass tagged a526/a527 `dag-schema` and hit 17 contract-level plus 35
schema matches, every one bare tag_overlap. The tag was wrong - the
position format is document schema, not DAG-node schema - and under
a511's own positions/schema/anchoring family the matches collapsed to
zero. A new wrinkle for the calibration: this time the wall had
diagnostic value, catching a semantic mis-tag before import. The cut
both ways goes into a519's evidence.

**The hand-edit slip (a522's fifth and sixth specimens).** The discharge
bookkeeping - a511's evidence append and the t0013 status flip - went
through raw python because the evidence-append front door (a522) remains
unbuilt. The whole-file re-dump used json.dumps defaults and
unicode-escaped every non-ascii character in the graph file; caught by
diff inspection, re-dumped clean. A front door would pin serialization
along with the append.

The one genuine adjudication (a473 flagged against a528, naming-tag
overlap only, no semantic contact) was user-decided proceed-unchanged
and recorded in a528's evidence. The position appendix's three claims
flipped to extracted with a503-floor refs (gloss + id + json line; no
card links - the card renderer is still a502 on the desk).

## Where things stand

Commit 15295fd carries the discharge; this close adds the evidence
appends and the chronicle. Gates green: schema verify 20 passed,
CLAUDE.md current. The desk stands at eleven and a511 leaves it - the
anchored-answer arc that opened 06-10 is now fully landed: position,
resolver, skill, format. What remains of that family on the desk is
infrastructure (a502 cards, a503 ref convention, a518 session-slug
resolution, a519 calibration, a522 the append front door). No blog
draft: routine discharge of designed work; the learnings are evidence
appends on existing items. The next session inherits a desk of eleven,
none blocked, the operator-decision items (a494, a495) still waiting on
the operator.
