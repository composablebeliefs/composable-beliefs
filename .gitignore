/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# ExUnit @tag :tmp_dir scratch
/tmp/

# Auto-captured session transcripts: crash-safety + reading, rewritten every turn,
# non-provenance (the nursery seeds are). Never committed.
/beliefs/nursery/threads/.sessions/

# Python bytecode cache (e.g. from the transcript hook)
__pycache__/
*.pyc
