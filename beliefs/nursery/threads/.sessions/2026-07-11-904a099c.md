# Session transcript - 904a099c-72f5-5bc5-ad9c-c8c6fb22fa27

> Auto-captured by the Stop hook. **Non-provenance** (see [index](index.md)) - the nursery seeds are the provenance. Substantive responses are kept; short pre-tool narration, reasoning, and tool calls are stripped.  
> Session `904a099c-72f5-5bc5-ad9c-c8c6fb22fa27` | 2026-07-11

## User

Recently there was work done creating some sort of adversarial code review process, are you able to find it?

## Assistant

Yes — it's the **council mechanism** (`/council`), a structured multi-agent adversarial review process designed on 2026-07-04 and minted into the graph on 2026-07-06.

**Where it lives:**
- **Design document:** `beliefs/nursery/council-mechanism.md` — the proto-belief covering the full mechanism
- **Design deliberation:** `beliefs/nursery/threads/2026-07-04-council-design.md`
- **Minted beliefs:** cb:b591 through cb:b594, all active prescriptions in `beliefs/cb/` (verified with `mix bs show cb:b591`)

**What it is:** the review analog of `/end` — a disciplined review-and-converge round that runs at the gate between "work drafted" and "work merged." One re-invocable skill with four motions:

- **`open`** — ensure a draft PR exists as the chamber, dispatch reviewer agents with a fixed adversarial brief plus the diff; findings land as PR review comments
- **`review`** — a fresh reviewer adds an independent auditor pass
- **`respond`** — the author replies to every finding with a disposition: agree / refute (with evidence) / defer; nothing silently dropped
- **`close`** — the convergence gate, modelled on `mix cb.adjudicate`: it refuses to converge while any finding is undispositioned, then distills the settled outcome into the target proto-belief plan

The four minted prescriptions encode the load-bearing rules: the medium is a draft PR with reviews as comments, never committed files (b591); the durable record is distilled, not dumped — raw exchange cited by commit/PR only (b592); close is a gate, not a dump (b593); and a dev-dev review round mints no thread — only operator-in-the-loop deliberation does (b594).

**Status:** the beliefs are minted, but the `/council` skill itself hasn't been built yet — the first round was deliberately hand-run (there's no skill while the skill is being designed), with the route-tagging audit as the founding worked example. Open items include whether `open` belongs in-scope for the skill, mechanical convergence detection, and where the thread-tiering rule ultimately lives.

Committed the hook-staged session render (`beliefs/nursery/threads/.sessions/2026-07-11-904a099c.md`) and pushed it to `claude/adversarial-code-review-f4arf6` as commit `f641bb6`. The working tree is clean now.

## User

Would it be possible to transfer this functionality to a different repo via a prompt?

## Assistant

Yes, and quite cleanly — because of what the mechanism actually is. Almost none of it is code. Here's the breakdown of what transfers and how.

**What the functionality consists of.** The council mechanism has no implementation to port: there's no `/council` skill built yet, no mix task, no hooks. The entire thing is *doctrine* — the design document (`beliefs/nursery/council-mechanism.md`) plus four prescriptions (cb:b591–b594). That's the best possible situation for prompt-transfer: the mechanism is already expressed as rules an agent follows, not machinery a repo has to install.

**What ports directly.** The four motions and both load-bearing rules are repo-agnostic:

- The draft PR as the review chamber; findings and responses are PR review comments, never committed files
- `open` dispatches reviewer agents with a fixed adversarial brief plus the diff
- `respond` forces a disposition on every finding — agree, refute with evidence, or defer — nothing silently dropped
- `close` is a gate, not a dump: it refuses to converge while any finding is undispositioned
- The durable record is distilled, not dumped: the raw exchange stays referenced by commit/PR, never copied into the tree

Any repo with GitHub PRs and an agent that can spawn subagents and post review comments can run all of that from a prompt alone.

**What needs re-pointing.** Two pieces are coupled to this repo's belief-graph infrastructure:

1. **The distillation target.** Here, `close` distills into a proto-belief's mint-manifest rows. A different repo doesn't have proto-beliefs, so the prompt has to name a substitute — a design doc, an ADR, an issue, a plan file. The *shape* of the distillation ports fine (settled decisions in one block, a dated "rejected: X because Y" block); only the destination changes.
2. **The provenance-tiering rule** (dev-dev rounds mint no thread) presupposes the thread doctrine. In a repo without threads it either drops out or simplifies to "the review round leaves no artifact in the tree beyond the distilled plan."

**The natural forms.** Three packaging options, in increasing durability: a one-shot prompt you paste into a session ("run a council round on this draft PR: ..."), a section in the target repo's `CLAUDE.md`, or a proper `/council` skill file in the target repo's `.claude/skills/` — which would arguably make the *other* repo the place the skill gets built first, with lessons flowing back here. The design doc even anticipates this pattern: the first council is hand-run, and a hand-run round is a legitimate founding example as long as it's labelled as such.

One caveat: the prompt should carry the close-gate and disposition discipline verbatim, because those are what make the process adversarial rather than decorative — a transfer that keeps "spawn reviewers, post comments" but drops "refuse to converge while any finding is undispositioned" loses the property the whole design exists for.

If you want, I can draft the portable prompt (or skill file) now — I'd just need to know the target repo, or I can write it repo-generic with the distillation target left as a fill-in.

Committed the hook-updated session render and pushed to `claude/adversarial-code-review-f4arf6` (`d0c38da`). Working tree is clean.

## User

Write the prompt, Repo generic, present as a code block for one click copy.

## Assistant

Here's the repo-generic council prompt. The one fill-in is the distillation target at the top; everything else runs as-is in any repo with GitHub PRs and subagent support.

````markdown
# Council: an adversarial review round on a draft PR

You are running a **council round**: a structured, adversarial, multi-agent
review of drafted work, driven to convergence and distilled into a plan.
The round runs at the gate between *work drafted* (committed to a branch,
not yet merged) and *work merged*.

**Distillation target:** <FILL IN — the document that receives the round's
settled outcome: a design doc, ADR, issue, or plan file. The target is a
curated document, not a transcript.>

## Ground rules (load-bearing — do not weaken these)

1. **The medium is a draft PR; reviews are comments, never committed files.**
   All findings, responses, and dispositions live as PR review comments.
   The only artifacts that ever land in the tree are the distilled plan and
   the code itself. Never run a round against main, and never replay a round
   that already happened elsewhere.
2. **The durable record is distilled, not dumped.** Settled decisions are
   written into the distillation target; rejected alternatives fold into a
   dated "rejected: X because Y" block in that same document; the raw
   exchange stays referenced by commit SHA / PR link and is never copied
   into the document.
3. **Close is a gate, not a dump.** The round cannot converge while any
   finding lacks a disposition. Silent omission is the failure mode the
   review exists to catch — never commit it yourself.

## The four motions

Run these in order (or resume at whichever motion the round has reached —
the PR itself is the shared state, so independent sessions can take turns).

### 1. open
- Ensure a **draft PR** exists for the branch under review; create one if
  needed. This PR is the chamber for the whole round.
- Dispatch one or more reviewer subagents. Each receives: the full diff,
  and this fixed adversarial brief:
  > You are an adversarial reviewer. Your job is to find what is wrong,
  > missing, unjustified, or fragile in this change — correctness bugs,
  > unstated assumptions, design flaws, gaps between the stated intent and
  > the code. Do not praise. Do not soften. Return a structured findings
  > report: each finding gets an ID (F1, F2, ...), a severity, the file/line
  > it anchors to, and a concrete failure scenario or reasoned objection.
- Post each reviewer's findings as PR review comments (line-anchored where
  possible, top-level otherwise), prefixed with their finding IDs.

### 2. review
- A fresh reviewer (new subagent, no context from motion 1) makes one
  independent pass over the diff with the same adversarial brief, and posts
  its findings the same way, continuing the finding-ID sequence.

### 3. respond
- Acting as the author, read every open finding and reply to each — in the
  finding's own comment thread — with exactly one disposition:
  - **agree** — the finding stands; state what will change.
  - **refute** — the finding is wrong; give evidence (code, tests, docs),
    not assertion.
  - **defer** — real but out of scope for this round; state where it goes
    (issue, follow-up plan) and file it there.
- Nothing is silently dropped. Every finding gets a reply.

### 4. close
- **The gate:** enumerate every finding from motions 1–2 and verify each
  has a disposition (agree / refute-with-evidence / defer-with-destination).
  If any finding is undispositioned, **refuse to close** — report which
  findings are open and stop. Do not summarize around them.
- Once the gate passes, distill:
  - Settled decisions (agreed findings and their resolutions) become
    concrete items in the distillation target.
  - Rejected alternatives become a dated "rejected: X because Y" block in
    the same document.
  - Cite the raw exchange by PR link and commit SHA only. Do not paste the
    transcript.
- Post a closing PR comment: the disposition tally (N agreed / N refuted /
  N deferred), a link to the updated distillation target, and the statement
  that the round is converged.
- Hand off: the distilled plan — not this conversation — is what the
  implementer receives.

## Provenance
- A council round is a dev-dev exchange: it leaves no artifact in the tree
  beyond the distilled plan and the code. Session notes, transcripts, and
  review dumps do not get committed.
- If this round is being run by hand (no tooling support yet), label it as
  hand-run in the closing comment. A hand-run round is a legitimate founding
  example only when labelled as such — do not manufacture receipts for
  machinery that was not in force.
````

Two usage notes: paste it with the diff already on a pushed branch (motion 1 needs something to open the draft PR against), and if the target repo has no natural distillation target yet, `docs/plans/<topic>.md` is a fine default to name in the fill-in.
