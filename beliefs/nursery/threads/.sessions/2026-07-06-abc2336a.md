# Session transcript - abc2336a-1744-5acb-a0e3-08e0da5d7545

> Auto-captured by the Stop hook. **Non-provenance** (see [index](index.md)) - the nursery seeds are the provenance. Substantive responses are kept; short pre-tool narration, reasoning, and tool calls are stripped.  
> Session `abc2336a-1744-5acb-a0e3-08e0da5d7545` | 2026-07-06

## User

Do a high level analysis of the state of this repo. Measure its effectiveness at its intended goals, its blue ocean potential, and design wuality.  In oarticular consider it as an eval ledger. Pull comps (use non-fable research agents) and list comparison and analysis

## Assistant

I'll analyze this from two directions at once: hands-on exploration of the repo (graph stats, code, git history, verification tooling), and parallel research agents running on Sonnet (non-Fable, per your ask) to pull comparables across three markets — agent memory systems, eval/observability ledgers, and academic prior art.

Let me kick off the research agents and start local exploration.

The local audit is complete; I'm now waiting on the three research agents (comps for agent-memory systems, eval/observability ledgers, and academic prior art) before writing the synthesis.

What I've established locally so far, in brief:

- **The repo has already pivoted to the eval-ledger framing.** The README leads with "an evidence ledger for LLM evaluation findings," and there's real machinery behind it: a neutral `run-manifest` v1 spec, a deterministic importer (`mix cb.import.eval`) that emits immutable observations only with hash-derived IDs and hard errors on silent rewrites, six machine-checkable methodology contracts (cross-ruler corroboration, raw-log provenance, ≥3 runs per verdict, LLM-judge human-validation, correction-as-supersession), and a zero-JS self-contained HTML audit-tree renderer that I just smoke-tested successfully.
- **Engineering hygiene is strong for its age**: 465 tests green, CI gates on warnings-as-errors, schema self-verification, a belief↔commit provenance verifier, and generated-doc freshness checks. ~12.7k lines of Elixir, one dependency, deterministic non-LLM read path.
- **It's very young and single-operator**: the whole repo is a two-week-old extraction (June 23–July 6, 2026), 229 of ~280 commits authored by Claude, with the worked eval collections living in a sibling repo not present here.
- **The flagship graph underuses its own composition machinery**: of 257 nodes, 196 are prescriptions and only 9 are inferences, with aggregations nearly absent — the "beliefs compose into conclusions" payoff is asserted more than demonstrated in the shipping graph.

I'll deliver the full analysis — effectiveness, design quality, blue-ocean assessment, and the comps table — once the research agents report back.

Committed the session render and pushed the branch. Still waiting on the three research agents (agent-memory comps, eval-ledger comps, academic prior art) — I'll synthesize the full analysis when they report back.

One of the research fan-out agents has reported: a detailed Letta/MemGPT profile (notably, Letta pivoted in 2026 to a git-backed memory filesystem — a striking convergence with CB's git-native design, but without typed claims or provenance schema). Still waiting on the remaining comps agents (the memory-systems synthesizer, the eval-ledger landscape, and the academic prior art) before writing the full analysis.
