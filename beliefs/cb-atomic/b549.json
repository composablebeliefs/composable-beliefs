{
  "id": "cb:b549",
  "type": "prescription",
  "kind": "action-item",
  "domain": "dev",
  "tags": [
    "backlog",
    "lifecycle:discrete",
    "docs",
    "provenance"
  ],
  "claim": "Generate the live-command transcript blocks in the eval worked example (docs/worked-example-eval-verdict.md) from the graph instead of hand-maintaining them. The doc pastes mix bs / mix cb.verify output verbatim, so it drifts whenever the artifact-scheme enum, the method: collection, or the sdl collection changes - this thread refreshed it twice (the verify counts, then the b043->b066 supersession section). Hand-pasted command output whose freshness depends on remembering to regenerate it is the b386 antipattern; a generator, or a doctest-style check that fails CI when a block goes stale, would render it live.",
  "artifact": "session:2026-06-23-worked-example-transcript-drift",
  "evidence": [
    {
      "date": "2026-06-23",
      "detail": "Authored in the worked-example transcript-drift thread; the arc (the doc refreshed twice in one thread, b043->b066 being the second pass) and the generate-not-hand-maintain rationale are narrated in the chronicle.",
      "artifact": "document:deprecated/chronicles/2026-06-23-the-transcript-wants-a-generator.md"
    },
    {
      "date": "2026-06-23",
      "detail": "Tradeoffs between the two paths the claim names, recorded so the choice need not be re-derived. GENERATOR (a mix task renders the doc's command blocks live from the graph): prevents drift outright since the committed doc is produced rather than hand-edited, but it must reproduce the doc's editorial curation rather than raw command dumps - blocks elide rows (e.g. '15 more schema PASS rows ... elided') and truncate claim columns - and it couples doc generation to the mix tooling plus a sibling belief-collections checkout, because the sdl/method blocks need --beliefs paths resolvable. CHECKER (a CI gate re-runs each fenced block and fails on mismatch): lighter, changes no authoring, makes silent drift impossible because the build goes red - but it only flags, a human still re-pastes, and it must normalize the curated/elided regions before diffing and resolve the cross-repo collections in CI. HYBRID is viable: generate verbatim-dump blocks, normalize-and-check the curated ones. Leaning (not decided): start with the checker - lowest authoring risk, stops drift immediately, the same normalization handles curation - and promote to a generator only if curation reproduction and cross-repo resolution prove cheap. Either path first needs belief-collections present in CI; that cross-repo dependency is the shared prerequisite.",
      "artifact": "document:deprecated/chronicles/2026-06-23-the-transcript-wants-a-generator.md"
    }
  ],
  "deps": [
    "cb:b386",
    "cb:b466"
  ],
  "status": "active",
  "created": "2026-06-23"
}
