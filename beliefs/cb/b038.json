{
  "id": "cb:b038",
  "type": "prescription",
  "kind": "schema",
  "domain": "system",
  "tags": [
    "dag-schema",
    "schema-discipline"
  ],
  "claim": "Schema discipline: belief provenance is carried by an artifact field; contract-grade implications carry contract:true with non-empty rules/invariants; the implication field is absent; enum-shaped fields (kind, domain, artifact-scheme) take values from b039/b040/b041 respectively.",
  "rules": [
    "The artifact field is the canonical provenance carrier on beliefs. Its value is null or a string matching scheme:id where scheme is declared by b040.",
    "The kind field's value is null or one of the values declared by b039.",
    "The domain field's value is null or one of the values declared by b041.",
    "The contract field is a boolean. It is allowed only on type: implication.",
    "contract: true is required iff rules or invariants are non-empty (bidirectional).",
    "The implication field is absent on all beliefs.",
    "Beliefs with kind: action-item have type: implication, contract: false, and empty rules/invariants.",
    "type: compound and type: implication are distinct types and are not merged.",
    "Artifacts (external documents, threads, files) are not promoted to belief nodes; they are referenced via the artifact field only."
  ],
  "invariants": [
    "For all beliefs b: b.contract == true implies b.type == 'implication'.",
    "For all beliefs b: ((b.rules // []) ++ (b.invariants // [])) non-empty iff b.contract == true.",
    "For all beliefs b: b.artifact == null OR b.artifact matches /^[a-z][a-z0-9_-]*:.+/.",
    "For all beliefs b: b.kind == null OR b.kind is in b039.enum.",
    "For all beliefs b: b.artifact == null OR scheme(b.artifact) is in b040.enum.",
    "For all beliefs b: b.domain == null OR b.domain is in b041.enum.",
    "For all beliefs b: b does not have an `implication` field.",
    "For all beliefs b with b.kind == 'action-item': b.type == 'implication' AND b.contract != true AND (b.rules // []) is empty AND (b.invariants // []) is empty."
  ],
  "artifact": "session:2026-05-15-dag-schema-discipline",
  "subjects": [
    {
      "ref": "the belief module",
      "type": "module"
    },
    {
      "ref": "a mix task",
      "type": "module"
    }
  ],
  "deps": [
    "cb:b397",
    "cb:b398",
    "cb:b399",
    "cb:b400",
    "cb:b401",
    "cb:b402",
    "cb:b403",
    "cb:b404",
    "cb:b405",
    "cb:b408"
  ],
  "status": "superseded",
  "superseded_by": "cb:b056",
  "created": "2026-05-15"
}
