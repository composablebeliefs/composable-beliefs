<!-- Generated by mix cb.generate.kb from the belief graph. Do not edit; run the task to regenerate. -->

# cb:b041

> Canonical enum of belief.domain values. Each value declared inline. The enum is closed.

**Type:** prescription (contract-grade) · **Kind:** enum-registry · **Domain:** system · **Status:** active
**Tags:** `dag-schema`, `enum`, `domain`
**Created:** 2026-05-15

**Artifact:** `session:2026-05-15-dag-schema-discipline`

## Invariants

- Exactly one rule entry, with field 'domain'.
- Each value matches /^[a-z][a-z0-9-]*$/.
- Values are unique within the entry.
- Every value has a corresponding entry in the definitions map.
- For all active beliefs b where b.domain is not null: b.domain is in values.

## Rules

```json
{
  "definitions": {
    "agent": "Claims about agent behavior, capability, or limitation.",
    "design": "Claims about system or process design.",
    "dev": "Claims about agent system development, DEV-mode work, and infrastructure-vs-operations distinctions.",
    "ops": "Claims about operational workflows, business processes, and domain entities.",
    "system": "Claims about the DAG, the agent, or the supporting infrastructure."
  },
  "field": "domain",
  "values": [
    "system",
    "design",
    "agent",
    "ops",
    "dev"
  ]
}
```

## Evidence

1. **2026-06-07** - reshaped rules from 'domain:<value> - <definition>' strings to a single {field,values,definitions} enum-registry entry; switched kind schema->enum-registry; representational, content-preserving (definitions retained verbatim).
   - artifact: `session:2026-06-07-cb-restructure-stage2`

## Depends on

- [cb:b397](b397.md) - Enum-shaped fields on beliefs (kind, domain, artifact-scheme, others as introduced) take values from sets declared in dedicated contracts.

## Subjects

- `a mix task` (module)

[Index](../README.md)
