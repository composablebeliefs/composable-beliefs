<!-- Generated by mix cb.generate.kb from the belief graph. Do not edit; run the task to regenerate. -->

# cb:b067

> Canonical enum of artifact URI schemes. Each scheme declared inline with its URI form. The enum is closed: no artifact value with a scheme outside this set is permitted on active beliefs.

**Type:** prescription (contract-grade) · **Kind:** enum-registry · **Domain:** system · **Status:** active
**Tags:** `dag-schema`, `enum`, `artifact-scheme`
**Created:** 2026-07-01

**Artifact:** `session:2026-07-01-schema-v3-execution`

## Invariants

- Exactly one rule entry, with field 'artifact-scheme'.
- Each value matches /^[a-z][a-z0-9-]*$/.
- Values are unique within the entry.
- Every value has a corresponding entry in the definitions map.
- For all active beliefs b where b.artifact is not null: scheme(b.artifact) is in values.

## Rules

```json
{
  "definitions": {
    "code": "An anchored site within a repository file. Form: code:<repo-relative-path>#<anchor>. Everything after the first '#' is one opaque literal substring anchor (the fixed-string grep target); an optional trailing '@<N>' selects the Nth match, and a literal trailing '@<digits>' is percent-encoded as '%40<digits>'. Line numbers are never stored - they resolve at render/run time. document: remains the whole-file reference.",
    "commit": "A git commit in a repository, cited as the change-event that implements or discharges a belief. Form: commit:<sha> where <sha> is the full 40-hex-char commit id (content-addressed, so the reference never drifts - the temporal analog of code:'s spatial anchor). Resolved via git (mix cb.verify.commits) in the host repository.",
    "document": "A repository file reference. Form: document:<repo-relative-path>. Files under ops/, dates/, lib/, etc.",
    "https": "An external URL. Form: https:<URL-rest>. Web pages, papers, external resources.",
    "plan": "A plan/spec/intent reference. Form: plan:<id-or-descriptor>.",
    "session": "A working session reference. Form: session:<date-or-descriptor>. Resolved via session transcript (persistence pipeline pending).",
    "source": "A cached source document reference. Form: source:<slug>. Documents stored under ops/sources/ or similar.",
    "user": "A direct user statement. Form: user:<name>:<date>. Internal authorship provenance."
  },
  "field": "artifact-scheme",
  "values": [
    "session",
    "user",
    "document",
    "source",
    "https",
    "plan",
    "code",
    "commit"
  ]
}
```

## Evidence

1. **2026-07-01** - Adds the commit: scheme, resolving cb:b545 as option 1 (operator decision 2026-07-01): the belief<->commit link becomes structural rather than prose convention. A prescription's discharge can cite the implementing commit as a typed artifact, CB.CommitLocator parses it, and mix cb.verify.commits dereferences every cited sha and checks Belief: commit trailers name live beliefs - CI-enforced both ways. The seven inherited schemes and all five invariants carry forward verbatim from cb:b066.
   - artifact: `session:2026-07-01-schema-v3-execution`
2. **2026-07-01** - Accepted via human adjudication against cb:b066. Reasoning: Single-scheme extension of the closed artifact-scheme enum, resolving cb:b545 as option 1 by operator decision (2026-07-01): add commit:<sha> so the belief<->commit link is structural and CI-enforceable, not a prose convention. The commit scheme names a git primitive, not a connector or product, so the b066 architectural invariant (core names no connector) is respected. All seven inherited schemes, definitions, and the five invariants carry forward verbatim; single-scheme supersession consistent with b397's batching discipline, the same shape as the b043->b066 move. Sibling contract-level matches in preflight (b039, b041, b050, b059) are topical overlap on dag-schema tags/subjects, not contradictions - this proposal touches only the artifact-scheme enum.
   - artifact: `adjudication:human:2026-07-01-schema-v3-execution`

## Depends on

- [cb:b397](b397.md) - Enum-shaped fields on beliefs (kind, domain, artifact-scheme, others as introduced) take values from sets declared in dedicated contracts.
- [cb:b398](b398.md) - A belief's artifact field holds a typed URI identifying the external referent the belief was derived from.
- [cb:b407](b407.md) - A URI scheme is the lowercase token preceding the first colon in a URI.
- [cb:b467](b467.md) - The cb-codepath plan set (plans/cb-codepath/) settles the code-anchoring design: a codepath is a cb collection; a claim anchors to a within-file site via code:<repo-relative-path>#...

## Depended on by

- [cb:b545](b545.md) - Decide whether to make the belief<->commit link structural by adding a commit:/git: artifact scheme to the closed enum (cb:b066), versus keeping it as the current unenforced prose ...
- [cb:b550](b550.md) - Anchored-position format: a position document carries a **Class:** header field; its stance decomposes into ### Claim: sections, each of which may carry one or more **Anchor:** lin...
- [cb:b563](b563.md) - Prospective commit-citation gate on the todo front door: mix cb.todo.close requires every new close to either cite its implementing commit (--commit <full-40-hex-sha>, validated by...

## Subjects

- `a mix task` (module)

## History

- Supersedes [cb:b066](b066.md)

[Index](../README.md)
