# Codepaths: beliefs anchored to code

This essay has been consolidated into [the guide](guide/README.md): the full treatment - the `code:` locator grammar, the render-spec contract, the narrate/assert gradient, inspection-only predicates, answer-time anchoring, and the shipped tour of CB's own pipeline - is [chapter 4, code, anchors, positions](guide/4-code.md).

Quick commands:

```sh
CB_BELIEFS=codepath/beliefs.json mix cb.render.codepath belief-pipeline   # tour the pipeline
CB_BELIEFS=codepath/beliefs.json mix cb.verify.codepath belief-pipeline   # test the pipeline
```

Design record and plans: `plans/cb-codepath/`.
